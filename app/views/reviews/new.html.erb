<h1>Writting a Review</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Add review", class: "btn btn-primary" %>
<% end %>
<%= link_to 'Back', restaurants_path %>

